# Copia de .env.bitwarden-preview con API local (misma Neon QA que Preview).
# Generar con: cd ../simplefactu && ./scripts/sync-local-env-from-qa.sh
#
# Uso: pnpm dev:stack  (requiere API local + túnel Postgres; ver abajo)

DATABASE_URL="postgresql://USER:PASS@ep-....neon.tech/neondb?sslmode=require"

SIMPLEFACTU_API_BASE_URL=http://localhost:3000/v1
SIMPLEFACTU_ADMIN_KEY="same-as-vps-qa-ADMIN_KEY"

# Clerk, VERIFACTU_*, etc. — mismos valores que .env.local / Preview
